# Secrets — never commit API keys or credentials
secrets/

# Go build artifacts
/gai
*.exe

# Lavish Editor review artifacts — scratch HTML, not source
.lavish/

# Agent team workflow state — per-run, not source
tasks.json